## 1. 布局重构

- [x] 1.1 重构 `file-server` 页面结构，明确左侧树工作区与右侧占位区的主次关系
- [x] 1.2 移除当前右侧内容列表中的主操作职责，避免页面出现双中心交互

## 2. 树工作区增强

- [x] 2.1 调整树渲染逻辑，让左侧树同时展示文件夹和文件节点
- [x] 2.2 为树节点补齐选中态、展开层级和轻量操作入口
- [x] 2.3 将新建文件夹、上传文件、删除目标等动作迁移到树工作区完成

## 3. 右侧占位区收束

- [x] 3.1 将右侧内容区改为空白占位态，并提供明确的预留说明文案
- [x] 3.2 确保主要文件操作不再依赖右侧区域作为入口

## 4. 验证与回归

- [x] 4.1 在浅色和深色主题下验证树工作区的视觉一致性
- [x] 4.2 验证树中新增、上传、删除流程仍正确调用现有文件服务接口
- [x] 4.3 运行 `file-server` 的 lint 与 build，修复因布局和交互调整产生的回归
