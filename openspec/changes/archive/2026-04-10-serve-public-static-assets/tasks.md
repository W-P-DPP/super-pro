## 1. 入口层静态资源挂载

- [x] 1.1 在 `general-server/app.ts` 中挂载 `general-server/public` 目录的静态资源服务
- [x] 1.2 调整中间件顺序，确保静态资源访问不进入 `/api` 的 JWT 和业务中间件链路

## 2. 行为验证

- [x] 2.1 补充后端集成测试，覆盖已存在静态资源文件可访问
- [x] 2.2 补充后端集成测试，覆盖缺失静态资源返回 404 且不受业务鉴权影响

## 3. 回归检查

- [x] 3.1 运行后端测试，确认静态资源服务与现有 `/api` 业务接口都正常
