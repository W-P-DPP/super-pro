## 1. 认证接入准备

- [x] 1.1 梳理 `login-template` 当前登录页对 `mock-auth.ts` 的依赖点，并确定需要保留或替换的表单状态结构
- [x] 1.2 新增认证请求与存储模块，定义登录请求参数、登录响应结构、统一错误解析和本地会话读写工具
- [x] 1.3 增加后端基础地址配置读取方案，约定 `VITE_API_BASE_URL` 的使用方式和缺失时的兜底提示

## 2. 登录页改造

- [x] 2.1 将 `LoginPage` 的提交逻辑改为调用真实登录接口，并接入加载、成功、失败状态
- [x] 2.2 按“记住当前设备”选项分别持久化认证信息到持久存储或会话存储
- [x] 2.3 收敛或禁用注册 mock 入口，确保页面只暴露与当前后端能力一致的交互
- [x] 2.4 在登录成功后展示当前用户信息与已登录状态反馈，不新增额外业务跳转页

## 3. 验证与收尾

- [x] 3.1 校验新增文案、成功态、错误态和禁用态在浅色与深色主题下均符合现有 token 与视觉语言
- [x] 3.2 执行 `pnpm --filter @super-pro/login-template lint` 与 `pnpm --filter @super-pro/login-template build`，修复接入过程中的类型或构建问题
- [x] 3.3 联调验证登录成功、账号密码错误、禁用用户、网络异常和重复提交场景
